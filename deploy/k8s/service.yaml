apiVersion: v1
kind: Service
metadata:
  name: memorix-server
  namespace: memorix
  labels:
    app: memorix-server
spec:
  selector:
    app: memorix-server
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
